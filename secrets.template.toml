# Copy this file to `secrets.toml` and replace placeholder values with real secrets.

# FBRef / FBR API credentials
fbref_api_key = "YOUR_FBREF_API_KEY"
# Optional: override the header name if your account uses a custom auth header
fbref_api_key_header = "x-api-key"
